#!/bin/bash

if [ "$1" == "--list" ] ; then
cat<<EOF
{
   "docker":
	{"hosts":["84.201.158.100","130.193.39.207"]}
}
EOF
elif [ "$1" == "--host" ]; then
  echo '{"_meta": {"hostvars": {}}}'
else
  echo "{ }"
fi
